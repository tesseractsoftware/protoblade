ProtoBlade can be installed as a pre-built binary for Windows, or via pip.

Pre-built binary
------------------------------------


WORK IN PROGRESS

Via Pip
------------------------------------

Create and activate a virtual environment, then install with pip:

.. code:: bash

    pip install protoblade
